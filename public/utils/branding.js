/** Fixed product identity. Legacy stored names and integration identifiers stay untouched. */
export const DEFAULT_APP_NAME = 'Vidamia';

export function displayAppName() {
  return DEFAULT_APP_NAME;
}
