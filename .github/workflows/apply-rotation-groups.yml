name: Apply rotation group implementation
on:
  push:
    branches: [feat/round-robin-task-assignment]
permissions:
  contents: write
jobs:
  apply:
    if: github.event.head_commit.message == 'build: apply rotation group implementation'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/round-robin-task-assignment
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Apply source patch
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f'anchor not found in {path}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1))

          # Migration 161: group identity, position, and cohort number live on each
          # occurrence. Existing rows remain ungrouped and behavior-compatible.
          replace_once('server/db.js', '''  {
    version: 160,
    description: 'Tasks: ordered round-robin assignment for recurring tasks',
    up: `
      ALTER TABLE tasks ADD COLUMN assignment_mode TEXT NOT NULL DEFAULT 'fixed'
        CHECK(assignment_mode IN ('fixed', 'round_robin'));
      ALTER TABLE tasks ADD COLUMN rotation_index INTEGER NOT NULL DEFAULT 0;

      CREATE TABLE IF NOT EXISTS task_rotation_members (
        task_id    INTEGER NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
        user_id    INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        sort_order INTEGER NOT NULL CHECK(sort_order >= 0),
        PRIMARY KEY (task_id, user_id),
        UNIQUE (task_id, sort_order)
      );
      CREATE INDEX idx_task_rotation_members_user ON task_rotation_members(user_id);
    `,
  },
];''', '''  {
    version: 160,
    description: 'Tasks: ordered round-robin assignment for recurring tasks',
    up: `
      ALTER TABLE tasks ADD COLUMN assignment_mode TEXT NOT NULL DEFAULT 'fixed'
        CHECK(assignment_mode IN ('fixed', 'round_robin'));
      ALTER TABLE tasks ADD COLUMN rotation_index INTEGER NOT NULL DEFAULT 0;

      CREATE TABLE IF NOT EXISTS task_rotation_members (
        task_id    INTEGER NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
        user_id    INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        sort_order INTEGER NOT NULL CHECK(sort_order >= 0),
        PRIMARY KEY (task_id, user_id),
        UNIQUE (task_id, sort_order)
      );
      CREATE INDEX idx_task_rotation_members_user ON task_rotation_members(user_id);
    `,
  },
  {
    version: 161,
    description: 'Tasks: synchronized rotation groups for recurring round-robin cohorts',
    up: `
      ALTER TABLE tasks ADD COLUMN rotation_group TEXT;
      ALTER TABLE tasks ADD COLUMN rotation_slot INTEGER NOT NULL DEFAULT 0 CHECK(rotation_slot >= 0);
      ALTER TABLE tasks ADD COLUMN rotation_cycle INTEGER NOT NULL DEFAULT 0 CHECK(rotation_cycle >= 0);
      CREATE INDEX idx_tasks_rotation_group_cycle
        ON tasks(rotation_group COLLATE NOCASE, rotation_cycle)
        WHERE rotation_group IS NOT NULL;
    `,
  },
];''')

          # Group validation/state helpers.
          replace_once('server/routes/tasks.js', '''function sameIdOrder(a, b) {
  return a.length === b.length && a.every((id, index) => id === b[index]);
}

function roundRobinConfigError''', '''function sameIdOrder(a, b) {
  return a.length === b.length && a.every((id, index) => id === b[index]);
}

function normalizeRotationGroup(value) {
  if (value === undefined || value === null) return null;
  const group = String(value).trim();
  return group || null;
}

function parseRotationSlot(value) {
  const slot = Number(value ?? 0);
  return Number.isInteger(slot) && slot >= 0 ? slot : null;
}

function currentRotationGroupState(d, rotationGroup, taskId = null) {
  if (!rotationGroup) return { rotationIndex: 0, rotationCycle: 0, peers: [] };

  let anchor = null;
  if (taskId) {
    const candidate = d.prepare('SELECT * FROM tasks WHERE id = ?').get(taskId);
    if (candidate?.rotation_group
        && candidate.rotation_group.localeCompare(rotationGroup, undefined, { sensitivity: 'accent' }) === 0) {
      anchor = candidate;
    }
  }
  if (!anchor) {
    anchor = d.prepare(`
      SELECT * FROM tasks
       WHERE rotation_group = ? COLLATE NOCASE AND parent_task_id IS NULL
       ORDER BY rotation_cycle DESC, id DESC LIMIT 1
    `).get(rotationGroup);
  }

  const rotationCycle = Number(anchor?.rotation_cycle || 0);
  const rotationIndex = Number(anchor?.rotation_index || 0);
  const params = [rotationGroup, rotationCycle];
  let sql = `SELECT * FROM tasks
              WHERE rotation_group = ? COLLATE NOCASE
                AND rotation_cycle = ? AND parent_task_id IS NULL`;
  if (taskId) { sql += ' AND id != ?'; params.push(Number(taskId)); }
  sql += ' ORDER BY rotation_slot ASC, id ASC';
  const peers = d.prepare(sql).all(...params);
  return { rotationIndex, rotationCycle, peers };
}

function rotationGroupConfigError(d, {
  taskId = null, joining = false, assignmentMode, rotationGroup, rotationSlot,
  rotationUserIds, recurrenceRule, recurrenceFromCompletion, dueDate, dueTime,
}) {
  if (!rotationGroup) return null;
  if (assignmentMode !== 'round_robin') return 'Rotation groups require round-robin assignment.';
  if (rotationGroup.length > 80) return 'Rotation group names are limited to 80 characters.';
  if (rotationSlot === null || rotationSlot >= rotationUserIds.length) {
    return 'Rotation group position must refer to a member in the round-robin list.';
  }

  const state = currentRotationGroupState(d, rotationGroup, taskId);
  if (joining && state.peers.some((peer) => peer.status !== 'open')) {
    return 'Cannot join a rotation group after its current cycle has started.';
  }
  for (const peer of state.peers) {
    if (!sameIdOrder(loadRotationUserIds(d, peer.id), rotationUserIds)) {
      return 'Every task in a rotation group must use the same ordered member list.';
    }
    if (peer.recurrence_rule !== recurrenceRule
        || Number(peer.recurrence_from_completion || 0) !== Number(recurrenceFromCompletion ? 1 : 0)
        || (peer.due_date ?? null) !== (dueDate ?? null)
        || (peer.due_time ?? null) !== (dueTime ?? null)) {
      return 'Every task in a rotation group must use the same recurrence schedule and due time.';
    }
    if (Number(peer.rotation_slot || 0) === rotationSlot) {
      return 'That position is already used in this rotation group.';
    }
  }
  return null;
}

function roundRobinConfigError''')

          # Create path: derive assignment from group phase + position.
          replace_once('server/routes/tasks.js', '''    const assignmentMode = req.body.assignment_mode ?? 'fixed';
    const rotationUserIds = parseRotationUserIds(req.body.rotation_user_ids);
    const rotationError = roundRobinConfigError(db.get(), {
      assignmentMode, isRecurring: !!is_recurring, recurrenceRule: recurrence_rule,
      parentTaskId: parent_task_id, rotationUserIds,
    });
    if (rotationError) return res.status(400).json({ error: rotationError, code: 400 });

    const requestedUserIds = parseAssignedTo(req.body.assigned_to);
    const userIds = assignmentMode === 'round_robin' ? [rotationUserIds[0]] : requestedUserIds;
    const firstUid = userIds[0] ?? null;
    const rotationIndex = 0;
''', '''    const assignmentMode = req.body.assignment_mode ?? 'fixed';
    const rotationUserIds = parseRotationUserIds(req.body.rotation_user_ids);
    const rotationError = roundRobinConfigError(db.get(), {
      assignmentMode, isRecurring: !!is_recurring, recurrenceRule: recurrence_rule,
      parentTaskId: parent_task_id, rotationUserIds,
    });
    if (rotationError) return res.status(400).json({ error: rotationError, code: 400 });

    const rotationGroup = assignmentMode === 'round_robin'
      ? normalizeRotationGroup(req.body.rotation_group)
      : null;
    const rotationSlot = rotationGroup ? parseRotationSlot(req.body.rotation_slot) : 0;
    const groupState = currentRotationGroupState(db.get(), rotationGroup);
    const groupError = rotationGroupConfigError(db.get(), {
      joining: !!rotationGroup && groupState.peers.length > 0,
      assignmentMode, rotationGroup, rotationSlot, rotationUserIds,
      recurrenceRule: recurrence_rule,
      recurrenceFromCompletion: recurrence_from_completion,
      dueDate: due_date, dueTime: due_time,
    });
    if (groupError) return res.status(400).json({ error: groupError, code: 400 });

    const requestedUserIds = parseAssignedTo(req.body.assigned_to);
    const rotationIndex = rotationGroup ? groupState.rotationIndex : 0;
    const rotationCycle = rotationGroup ? groupState.rotationCycle : 0;
    const activeRotationPosition = assignmentMode === 'round_robin'
      ? (rotationIndex + (rotationGroup ? rotationSlot : 0)) % rotationUserIds.length
      : 0;
    const userIds = assignmentMode === 'round_robin'
      ? [rotationUserIds[activeRotationPosition]]
      : requestedUserIds;
    const firstUid = userIds[0] ?? null;
''')

          replace_once('server/routes/tasks.js', '''           recurrence_from_completion, assignment_mode, rotation_index, points, visibility, countdown, locked)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
''', '''           recurrence_from_completion, assignment_mode, rotation_index, rotation_group, rotation_slot, rotation_cycle,
           points, visibility, countdown, locked)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
''')
          replace_once('server/routes/tasks.js', '''        is_recurring ? 1 : 0, recurrence_rule, recurrence_from_completion ? 1 : 0,
        assignmentMode, rotationIndex, points, visibility, countdown ? 1 : 0, req.body.locked ? 1 : 0
''', '''        is_recurring ? 1 : 0, recurrence_rule, recurrence_from_completion ? 1 : 0,
        assignmentMode, rotationIndex, rotationGroup, rotationSlot || 0, rotationCycle,
        points, visibility, countdown ? 1 : 0, req.body.locked ? 1 : 0
''')

          # Edit path: preserve group cohort, permit joining an open group, and
          # reject changes that would desynchronize a partially completed cohort.
          replace_once('server/routes/tasks.js', '''    const requestedUserIds = req.body.assigned_to !== undefined
      ? parseAssignedTo(req.body.assigned_to)
      : assignedBefore;
    let rotationIndex = 0;
    let userIds;
    if (assignmentMode === 'round_robin') {
      const oldCurrent = Number(task.assigned_to);
      const oldPosition = rotationUserIds.indexOf(oldCurrent);
      rotationIndex = task.assignment_mode === 'round_robin' && oldPosition >= 0 ? oldPosition : 0;
      userIds = [rotationUserIds[rotationIndex]];
    } else {
      userIds = requestedUserIds;
    }
    const firstUid = userIds[0] ?? null;
''', '''    const requestedRotationGroup = req.body.rotation_group !== undefined
      ? normalizeRotationGroup(req.body.rotation_group)
      : normalizeRotationGroup(task.rotation_group);
    const rotationGroup = assignmentMode === 'round_robin' ? requestedRotationGroup : null;
    if (task.rotation_group && rotationGroup
        && task.rotation_group.localeCompare(rotationGroup, undefined, { sensitivity: 'accent' }) !== 0) {
      return res.status(400).json({ error: 'Move a grouped task by removing it from the group first.', code: 400 });
    }
    const rotationSlot = rotationGroup
      ? (req.body.rotation_slot !== undefined ? parseRotationSlot(req.body.rotation_slot) : Number(task.rotation_slot || 0))
      : 0;
    const joiningGroup = !!rotationGroup && !task.rotation_group;
    const groupState = currentRotationGroupState(db.get(), rotationGroup, task.rotation_group ? task.id : null);
    const groupError = rotationGroupConfigError(db.get(), {
      taskId: task.rotation_group ? task.id : null,
      joining: joiningGroup,
      assignmentMode, rotationGroup, rotationSlot, rotationUserIds,
      recurrenceRule: recurrence_rule,
      recurrenceFromCompletion: recurrence_from_completion,
      dueDate: due_date, dueTime: due_time,
    });
    if (groupError) return res.status(400).json({ error: groupError, code: 400 });
    if (task.rotation_group && Number(task.rotation_slot || 0) !== rotationSlot
        && groupState.peers.some((peer) => peer.status !== 'open')) {
      return res.status(409).json({ error: 'Cannot change a rotation-group position after its current cycle has started.', code: 409 });
    }

    const requestedUserIds = req.body.assigned_to !== undefined
      ? parseAssignedTo(req.body.assigned_to)
      : assignedBefore;
    let rotationIndex = 0;
    let rotationCycle = 0;
    let userIds;
    if (assignmentMode === 'round_robin') {
      if (rotationGroup) {
        rotationIndex = groupState.rotationIndex;
        rotationCycle = groupState.rotationCycle;
        userIds = [rotationUserIds[(rotationIndex + rotationSlot) % rotationUserIds.length]];
      } else {
        const oldCurrent = Number(task.assigned_to);
        const oldPosition = rotationUserIds.indexOf(oldCurrent);
        rotationIndex = task.assignment_mode === 'round_robin' && oldPosition >= 0 ? oldPosition : 0;
        userIds = [rotationUserIds[rotationIndex]];
      }
    } else {
      userIds = requestedUserIds;
    }
    const firstUid = userIds[0] ?? null;
''')

          replace_once('server/routes/tasks.js', '''        assignment_mode: assignmentMode,
        countdown: countdown ? 1 : 0, points, visibility,
''', '''        assignment_mode: assignmentMode,
        rotation_group: rotationGroup, rotation_slot: rotationSlot || 0,
        countdown: countdown ? 1 : 0, points, visibility,
''')

          replace_once('server/routes/tasks.js', '''          assignment_mode = ?, rotation_index = ?, points = ?, visibility = ?, countdown = ?, locked = ?
        WHERE id = ?
      `).run(title.trim(), description, category, priority,
             status, start_date, due_date, due_time, firstUid,
             is_recurring ? 1 : 0, recurrence_rule, recurrence_from_completion ? 1 : 0,
             assignmentMode, rotationIndex, points, visibility, countdown ? 1 : 0, locked, req.params.id);
''', '''          assignment_mode = ?, rotation_index = ?, rotation_group = ?, rotation_slot = ?, rotation_cycle = ?,
          points = ?, visibility = ?, countdown = ?, locked = ?
        WHERE id = ?
      `).run(title.trim(), description, category, priority,
             status, start_date, due_date, due_time, firstUid,
             is_recurring ? 1 : 0, recurrence_rule, recurrence_from_completion ? 1 : 0,
             assignmentMode, rotationIndex, rotationGroup, rotationSlot || 0, rotationCycle,
             points, visibility, countdown ? 1 : 0, locked, req.params.id);
''')

          # Group-aware undo. Existing independent behavior stays in the original helper.
          replace_once('server/routes/tasks.js', 'function discardRecurrenceFollowup(taskId) {', 'function discardRecurrenceFollowupSingle(taskId) {')
          replace_once('server/routes/tasks.js', '''  db.get().prepare('DELETE FROM tasks WHERE id = ?').run(followup.id);
  return queued;
}

/**
 * Der Vorlauf gehört zum Durchlauf''', '''  db.get().prepare('DELETE FROM tasks WHERE id = ?').run(followup.id);
  return queued;
}

function discardRecurrenceFollowup(taskId) {
  const source = db.get().prepare('SELECT * FROM tasks WHERE id = ?').get(taskId);
  if (!source?.rotation_group) return discardRecurrenceFollowupSingle(taskId);

  const cohort = db.get().prepare(`
    SELECT * FROM tasks
     WHERE rotation_group = ? COLLATE NOCASE AND rotation_cycle = ? AND parent_task_id IS NULL
     ORDER BY rotation_slot ASC, id ASC
  `).all(source.rotation_group, source.rotation_cycle);
  const followups = cohort.map((member) => recurrenceFollowupOf(member.id));
  // Group deletion is all-or-nothing. A missing or touched next occurrence means
  // the whole generated cohort is preserved.
  if (!cohort.length || followups.some((f) => !f || f.status !== 'open')) return 0;
  if (followups.some((f) => isFollowupSubtasksTouched(f) || recurrenceFollowupOf(f.id))) return 0;

  let queued = 0;
  db.get().transaction(() => {
    for (const followup of followups) queued += queueTodoDeletion('tasks', followup) ? 1 : 0;
    const placeholders = followups.map(() => '?').join(',');
    db.get().prepare(`DELETE FROM tasks WHERE id IN (${placeholders})`).run(...followups.map((f) => f.id));
  })();
  return queued;
}

/**
 * Der Vorlauf gehört zum Durchlauf''')

          # Single-occurrence generator now carries group metadata and applies slot offset.
          replace_once('server/routes/tasks.js', 'function spawnRecurrenceFollowup(task) {', 'function spawnRecurrenceFollowupSingle(task) {')
          replace_once('server/routes/tasks.js', '''  const nextAssignedTo = roundRobin
    ? rotationUserIds[nextRotationIndex]
    : (task.assigned_to ?? existingAssignments[0] ?? null);
''', '''  const nextAssignedTo = roundRobin
    ? rotationUserIds[(nextRotationIndex + Number(task.rotation_group ? task.rotation_slot || 0 : 0)) % rotationUserIds.length]
    : (task.assigned_to ?? existingAssignments[0] ?? null);
''')
          replace_once('server/routes/tasks.js', '''        assignment_mode, rotation_index, points, visibility, recurrence_from_completion, countdown, recurrence_origin_id)
      VALUES (?, ?, ?, ?, 'open', ?, ?, ?, ?, ?, 1, ?, ?, ?, ?, ?, ?, ?, ?)
''', '''        assignment_mode, rotation_index, rotation_group, rotation_slot, rotation_cycle,
        points, visibility, recurrence_from_completion, countdown, recurrence_origin_id)
      VALUES (?, ?, ?, ?, 'open', ?, ?, ?, ?, ?, 1, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
''')
          replace_once('server/routes/tasks.js', '''      task.recurrence_rule, task.assignment_mode || 'fixed', nextRotationIndex, task.points, task.visibility,
      // Ohne das Mitnehmen fiele die Serie''', '''      task.recurrence_rule, task.assignment_mode || 'fixed', nextRotationIndex,
      task.rotation_group || null, Number(task.rotation_slot || 0),
      Number(task.rotation_cycle || 0) + (task.rotation_group ? 1 : 0),
      task.points, task.visibility,
      // Ohne das Mitnehmen fiele die Serie''')

          # Wrapper advances a grouped cohort only after every source occurrence is done.
          replace_once('server/routes/tasks.js', '''  })();
}

// --------------------------------------------------------
// PATCH /api/v1/tasks/:id/status''', '''  })();
}

function spawnRecurrenceFollowup(task) {
  if (!task?.rotation_group) return spawnRecurrenceFollowupSingle(task);

  const cohort = db.get().prepare(`
    SELECT * FROM tasks
     WHERE rotation_group = ? COLLATE NOCASE AND rotation_cycle = ? AND parent_task_id IS NULL
     ORDER BY rotation_slot ASC, id ASC
  `).all(task.rotation_group, task.rotation_cycle);
  if (!cohort.length || cohort.some((member) => member.status !== 'done')) return;
  if (cohort.some((member) => recurrenceFollowupOf(member.id))) return;

  const roster = loadRotationUserIds(db.get(), cohort[0].id);
  if (roster.length < 2) throw new Error('Rotation group has no valid member roster.');
  for (const member of cohort) {
    if (!sameIdOrder(loadRotationUserIds(db.get(), member.id), roster)
        || member.recurrence_rule !== cohort[0].recurrence_rule
        || Number(member.recurrence_from_completion || 0) !== Number(cohort[0].recurrence_from_completion || 0)
        || (member.due_date ?? null) !== (cohort[0].due_date ?? null)
        || (member.due_time ?? null) !== (cohort[0].due_time ?? null)
        || Number(member.rotation_index || 0) !== Number(cohort[0].rotation_index || 0)) {
      throw new Error('Rotation group cohort is inconsistent; refusing a partial advance.');
    }
  }

  // Nested transaction becomes a savepoint when called from PUT/PATCH. Either
  // every next position is generated or none are.
  db.get().transaction(() => {
    for (const member of cohort) spawnRecurrenceFollowupSingle(member);
  })();
}

// --------------------------------------------------------
// PATCH /api/v1/tasks/:id/status''')

          # Front-end group fields inside the round-robin panel.
          replace_once('public/pages/tasks.js', '''  const rotationIds = task?.rotation_user_ids ?? [];
  const assignmentMode = task?.assignment_mode || 'fixed';
''', '''  const rotationIds = task?.rotation_user_ids ?? [];
  const assignmentMode = task?.assignment_mode || 'fixed';
  const rotationGroup = task?.rotation_group || '';
  const rotationPosition = Number(task?.rotation_slot ?? 0) + 1;
''')
          replace_once('public/pages/tasks.js', '''      <div id="task-round-robin-assignment" class="form-group" style="margin-top:var(--space-4)"${assignmentMode === 'round_robin' && !isSoloHousehold() ? '' : ' hidden'}>
        ${renderUserRotationOrder(users, rotationIds)}
      </div>
''', '''      <div id="task-round-robin-assignment" class="form-group" style="margin-top:var(--space-4)"${assignmentMode === 'round_robin' && !isSoloHousehold() ? '' : ' hidden'}>
        ${renderUserRotationOrder(users, rotationIds)}
        <div style="margin-top:var(--space-3)">
          <label class="label" for="task-rotation-group">Rotation group <span class="text-muted">(optional)</span></label>
          <input class="input" id="task-rotation-group" name="rotation_group" maxlength="80"
                 value="${esc(rotationGroup)}" placeholder="e.g. Shower order">
          <p class="task-field-hint">Tasks with the same group advance together only after every position in the current cycle is complete.</p>
        </div>
        <div style="margin-top:var(--space-3)">
          <label class="label" for="task-rotation-position">Group position</label>
          <input class="input" id="task-rotation-position" name="rotation_position" type="number" min="1"
                 value="${rotationPosition}">
          <p class="task-field-hint">Position 1 uses the first person, position 2 the second, and so on. Each new cycle shifts all positions by one.</p>
        </div>
      </div>
''')
          replace_once('public/pages/tasks.js', '''  const assignmentMode = form.querySelector('#task-assignment-mode')?.value || 'fixed';
  const rotationUserIds = getRotationUserIds(form);
''', '''  const assignmentMode = form.querySelector('#task-assignment-mode')?.value || 'fixed';
  const rotationUserIds = getRotationUserIds(form);
  const rotationGroup = form.querySelector('#task-rotation-group')?.value.trim() || '';
  const rotationPosition = Number(form.querySelector('#task-rotation-position')?.value || 1);
''')
          replace_once('public/pages/tasks.js', '''    assignment_mode: assignmentMode,
    rotation_user_ids: assignmentMode === 'round_robin' ? rotationUserIds : [],
    visibility:''', '''    assignment_mode: assignmentMode,
    rotation_user_ids: assignmentMode === 'round_robin' ? rotationUserIds : [],
    rotation_group: assignmentMode === 'round_robin' && rotationGroup ? rotationGroup : null,
    rotation_slot: assignmentMode === 'round_robin' && rotationGroup ? rotationPosition - 1 : 0,
    visibility:''')
          replace_once('public/pages/tasks.js', '''    if (rotationUserIds.length < 2) { resetSubmit('Choose at least two members for the round-robin rotation.'); return; }
  }
''', '''    if (rotationUserIds.length < 2) { resetSubmit('Choose at least two members for the round-robin rotation.'); return; }
    if (rotationGroup && (!Number.isInteger(rotationPosition) || rotationPosition < 1 || rotationPosition > rotationUserIds.length)) {
      resetSubmit('Rotation group position must be between 1 and the number of rotation members.'); return;
    }
  }
''')

          # Tests: cohort waits, advances atomically, and undo is all-or-nothing.
          p = Path('test/test-task-round-robin.js')
          text = p.read_text()
          if "rotation group advances the whole cohort atomically" not in text:
              text += r'''

test('rotation group advances the whole cohort atomically and preserves slot offsets', async () => {
  const due = new Date().toISOString().slice(0, 10);
  const group = `Shower order ${Date.now()}`;
  const common = {
    due_date: due,
    is_recurring: 1,
    recurrence_rule: 'FREQ=DAILY',
    assignment_mode: 'round_robin',
    rotation_user_ids: [grace, eleanor, frank],
    rotation_group: group,
  };

  const first = await call('POST', '', { ...common, title: 'Shower - 1st', rotation_slot: 0 });
  const second = await call('POST', '', { ...common, title: 'Shower - 2nd', rotation_slot: 1 });
  const third = await call('POST', '', { ...common, title: 'Shower - 3rd', rotation_slot: 2 });
  assert.equal(first.status, 201, JSON.stringify(first.body));
  assert.equal(second.status, 201, JSON.stringify(second.body));
  assert.equal(third.status, 201, JSON.stringify(third.body));
  assert.equal(first.body.data.assigned_to, grace);
  assert.equal(second.body.data.assigned_to, eleanor);
  assert.equal(third.body.data.assigned_to, frank);

  await call('PATCH', `/${first.body.data.id}/status`, { status: 'done' });
  assert.equal(followupOf(first.body.data.id), undefined);
  await call('PATCH', `/${second.body.data.id}/status`, { status: 'done' });
  assert.equal(followupOf(second.body.data.id), undefined);
  await call('PATCH', `/${third.body.data.id}/status`, { status: 'done' });

  const nextFirst = followupOf(first.body.data.id);
  const nextSecond = followupOf(second.body.data.id);
  const nextThird = followupOf(third.body.data.id);
  assert.ok(nextFirst && nextSecond && nextThird);
  assert.equal(nextFirst.rotation_cycle, 1);
  assert.equal(nextSecond.rotation_cycle, 1);
  assert.equal(nextThird.rotation_cycle, 1);
  assert.equal(nextFirst.rotation_index, 1);
  assert.equal(nextFirst.assigned_to, eleanor);
  assert.equal(nextSecond.assigned_to, frank);
  assert.equal(nextThird.assigned_to, grace);

  // Reopening any source member removes the whole untouched generated cohort.
  const reopened = await call('PATCH', `/${second.body.data.id}/status`, { status: 'open' });
  assert.equal(reopened.status, 200);
  assert.equal(followupOf(first.body.data.id), undefined);
  assert.equal(followupOf(second.body.data.id), undefined);
  assert.equal(followupOf(third.body.data.id), undefined);
});

test('rotation group rejects roster mismatch and duplicate positions', async () => {
  const due = new Date().toISOString().slice(0, 10);
  const group = `Validation group ${Date.now()}`;
  const first = await call('POST', '', {
    title: 'Grouped first', due_date: due, is_recurring: 1, recurrence_rule: 'FREQ=DAILY',
    assignment_mode: 'round_robin', rotation_user_ids: [grace, eleanor, frank],
    rotation_group: group, rotation_slot: 0,
  });
  assert.equal(first.status, 201);

  const duplicate = await call('POST', '', {
    title: 'Duplicate slot', due_date: due, is_recurring: 1, recurrence_rule: 'FREQ=DAILY',
    assignment_mode: 'round_robin', rotation_user_ids: [grace, eleanor, frank],
    rotation_group: group, rotation_slot: 0,
  });
  assert.equal(duplicate.status, 400);

  const mismatch = await call('POST', '', {
    title: 'Mismatched roster', due_date: due, is_recurring: 1, recurrence_rule: 'FREQ=DAILY',
    assignment_mode: 'round_robin', rotation_user_ids: [eleanor, grace, frank],
    rotation_group: group, rotation_slot: 1,
  });
  assert.equal(mismatch.status, 400);
});

test('rotation group undo preserves the entire next cohort when one followup is touched', async () => {
  const due = new Date().toISOString().slice(0, 10);
  const group = `Touched group ${Date.now()}`;
  const common = {
    due_date: due, is_recurring: 1, recurrence_rule: 'FREQ=DAILY', assignment_mode: 'round_robin',
    rotation_user_ids: [grace, eleanor], rotation_group: group,
  };
  const a = await call('POST', '', { ...common, title: 'A', rotation_slot: 0 });
  const b = await call('POST', '', { ...common, title: 'B', rotation_slot: 1 });
  await call('PATCH', `/${a.body.data.id}/status`, { status: 'done' });
  await call('PATCH', `/${b.body.data.id}/status`, { status: 'done' });
  const nextA = followupOf(a.body.data.id);
  const nextB = followupOf(b.body.data.id);
  assert.ok(nextA && nextB);

  // An added subtask is user work according to Yuvomi's existing safe-undo rule.
  db.prepare(`INSERT INTO tasks (title, category, priority, status, created_by, parent_task_id, visibility)
              VALUES ('Touched child', 'misc', 'none', 'open', ?, ?, 'all')`).run(admin, nextA.id);

  await call('PATCH', `/${a.body.data.id}/status`, { status: 'open' });
  assert.ok(db.prepare('SELECT id FROM tasks WHERE id = ?').get(nextA.id));
  assert.ok(db.prepare('SELECT id FROM tasks WHERE id = ?').get(nextB.id));
});
'''
              p.write_text(text)

          # Wire the existing test into package.json deterministically.
          import json
          p = Path('package.json')
          pkg = json.loads(p.read_text())
          pkg['scripts']['test:task-round-robin'] = 'node --experimental-sqlite --test test/test-task-round-robin.js'
          anchor = 'npm run test:tasks-recurrence'
          if 'npm run test:task-round-robin' not in pkg['scripts']['test']:
              pkg['scripts']['test'] = pkg['scripts']['test'].replace(anchor, anchor + ' && npm run test:task-round-robin')
          p.write_text(json.dumps(pkg, indent=2) + '\n')

          # Documentation.
          p = Path('docs/ROUND_ROBIN_TASKS.md')
          text = p.read_text()
          if '## Rotation groups' not in text:
              text += '''\n\n## Rotation groups\n\nRecurring round-robin tasks can optionally share a **Rotation group**. Give each task in the group the same ordered member list and recurrence schedule, then assign each one a unique **Group position**. The group waits until every task in the current cycle is complete, then creates the entire next cycle atomically and shifts every position to the next member.\n\nFor a shower order with Grace, Eleanor, and Frank, create three daily tasks in the same group with positions 1, 2, and 3. The assignments advance as a cohort: `Grace / Eleanor / Frank`, then `Eleanor / Frank / Grace`, then `Frank / Grace / Eleanor`.\n\nReopening a task from the completed source cycle removes the entire generated next cycle only when all of those generated tasks are still safe to discard. If any generated task contains work under Yuvomi's existing recurrence safety checks, none of the group is deleted.\n'''
              p.write_text(text)
          PY
      - name: Install dependencies
        run: npm ci
      - name: Run focused compatibility tests
        run: |
          npm run test:task-round-robin
          npm run test:tasks-recurrence
          npm run test:multi-assignment
          npm run test:migrations-append-only
          npm run test:schema-reconcile
          npm run test:schema-mirror
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/db.js server/routes/tasks.js public/pages/tasks.js test/test-task-round-robin.js package.json docs/ROUND_ROBIN_TASKS.md
          git commit -m "feat(tasks): add synchronized rotation groups"
          git push origin HEAD:feat/round-robin-task-assignment