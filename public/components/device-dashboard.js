// Compatibility module for cached imports. Paired devices use the canonical
// dashboard and Task screens; permissions and projections remain server-owned.
export { render } from '/pages/dashboard.js';
